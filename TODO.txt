## V1 — Ship a complete, demoable tool
##   Status: MOSTLY DONE

- ✅ implement 5 more interpreters (SQL, URL, file path, error message, annotation)     P0  DONE
  → SqlConstantUsageInterpreter, UrlResourceConstantUsageInterpreter,
    FilePathConstantUsageInterpreter, ErrorMessageConstantUsageInterpreter,
    AnnotationConstantUsageInterpreter (config key was replaced by annotation)

- implement the constant diff analysis (compare two versions of the same class)        P0
  → ~15-20 hrs (the hard part: designing the diff model + a new endpoint +
    querying Postgres for two versions via the existing snapshot/constant/usage repos;
    the storage layer already supports versioned lookups)

## V2 — Depth features
##   At 10 hrs/week: ~4-6 weeks | At 20 hrs/week: ~2-3 weeks

- implement similar constant detection                                                  P1
  → ~12-15 hrs (string similarity algo + new endpoint + Solr or Postgres query;
    Solr's MLT could help but may need a custom approach for constant values)

- implement the constant split analysis                                                 P1
  → ~10-15 hrs (detect when one constant in version N became multiple in N+1;
    depends on diff being done first — reuses that infrastructure)

- implement the constant merge analysis                                                 P1
  → ~8-10 hrs (reverse of split; faster if split is done first, same patterns)

- add Micrometer/Prometheus metrics for analysis throughput + latency                    P2
  → ~4-6 hrs (add dependency, @Timed on key methods, /actuator/prometheus endpoint)

## V3 — Polish

- ✅ add a WebUI to display full analysis results (beyond search)                        P2  DONE
  → search-ui module: React 19 + TypeScript + Tailwind CSS v4
    fuzzy search, class lookup, file upload (class/JAR/config), version management

- debug why terraform fails to create the app docker image                              P3
  → ~2-4 hrs (likely just the image reference in main.tf)

- add exception-flow analysis to handle exception handling code                         P4
  → ~15-20 hrs (complex bytecode work — handler starts are tracked in
    SuccessorBuilder but the semantic interpretation isn't wired)

- ✅ split lib into model and bytecode analysis, to have a hook for Java source analysis P4  DONE
  → split into constant-extractor-api (model + SPI), constant-extractor-bytecode
    (JVM analysis), and constant-extractor-config-file (YAML/properties)

## Remaining estimate: ~65-90 hrs
##   V1 remaining (diff only): ~15-20 hrs
##   V2: ~34-46 hrs
##   V3 remaining: ~17-24 hrs
