POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
SPRING_R2DBC_USERNAME=postgres
SPRING_R2DBC_PASSWORD=postgres
SPRING_FLYWAY_USER=postgres
SPRING_FLYWAY_PASSWORD=postgres
SOLR_HOST_PATH=./.solr_data
CORS_ALLOWED_ORIGINS=http://localhost:5173,https://tracker.gg-dev.org

# Optionally pin to locally built images (defaults to GHCR if unset)
# APP_IMAGE=constant-tracker-app:local
# OPS_TOOL_IMAGE=ops-tool:latest

# Demo user credentials — used by the seed-auth-user and seed commands
DEMO_USERNAME=demo
DEMO_PASSWORD=change_me
AUTH_ENABLED=true
